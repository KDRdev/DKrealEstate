version: '3'
    
services:
  db:
    image: postgres
    environment:
      - POSTGRES_DB=
      - POSTGRES_USER=
      - POSTGRES_PASSWORD=
  web:
    build: .
    command: python manage.py runserver
    volumes:
      - .:/DKRealEstate
    ports:
      - "80:8000"
    depends_on:
      - db
